{
  "name": "checklog-monorepo",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "dev:app": "turbo dev --filter=@checklog/app",
    "dev:admin": "turbo dev --filter=@checklog/admin",
    "dev:web": "turbo dev --filter=@checklog/web",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  },
  "packageManager": "pnpm@11.7.0",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
