// =============================================================================
// spotcare.kr MVP — NextAuth API 라우트 핸들러
// Credentials Provider 의 CSRF/세션/콜백 엔드포인트를 노출한다.
// =============================================================================

import { handlers } from '@/auth'

export const { GET, POST } = handlers
