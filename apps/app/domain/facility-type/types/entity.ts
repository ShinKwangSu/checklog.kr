// =============================================================================
// facility-type 도메인 — 엔티티 타입
// =============================================================================
// 비밀 컬럼이 없어 DB Row 타입을 그대로 도메인 엔티티로 사용한다(별도 DTO 매핑 없음).
// =============================================================================

export type { FacilityType } from '@checklog/database'
