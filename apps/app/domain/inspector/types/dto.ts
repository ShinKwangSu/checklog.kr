// =============================================================================
// inspector 도메인 — 입력 DTO
// =============================================================================

/** 점검자 생성/수정 공통 입력 (빈 문자열은 액션에서 null 로 정규화) */
export type InspectorWriteInput = {
  name: string
  phone: string | null
  email: string | null
}
