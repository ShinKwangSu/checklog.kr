// =============================================================================
// facility 도메인 — 엔티티 타입
// =============================================================================
// 시설 도메인은 비밀 컬럼이 없으므로 DB Row 타입을 그대로 도메인 엔티티로 사용한다.
// (account 도메인처럼 password_hash 를 걸러낼 필요가 없어 별도 DTO 매핑을 두지 않는다.)
// 컴포넌트도 이 snake_case Row 타입을 그대로 소비한다.
// =============================================================================

export type { Facility, FacilityWithChecklists } from '@checklog/database'
